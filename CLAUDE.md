# Project context — VTSU Lyndon Alumni Council site

> Auto-loaded by Claude Code. If you're a future Claude session, read this before editing.
>
> **▶ HANDOFF — last session 2026-08-05. The Homecoming registration hierarchy is live across web and print:** registration actions now use a prominent rust-red treatment on the site and flyer; the Events card directs visitors to **Register for the Full Weekend** and places the Homecoming Poster beneath it; and its copy names all three registration paths (golf, welcome reception, banquet). The popup and Events card still omit the merchandise promotion, while the flyer again carries a compact **Lyndon gear** callout with an intentionally inactive green **Lyndon Store** pill pending Mark's URL. Flyer golf links to the live Blackbaud form; reception and banquet say **“RSVP: Let us know you're coming”** but remain intentionally unlinked until Sara provides URLs. Both flyer and poster QRs target **`https://lyndonalumni.com/homecoming-2026`**. The flyer export is a fresh one-page US Letter file and its PDF QR decodes at 144/192/300 DPI. Golf registration + sponsorship are live; the 2026 Athletic HOF class is posted; internal context docs remain blocked via `_redirects`; and the **Aldrich House / Manor Vail Society section** (`#aldrich`, after the history timeline) is live with a redacted preservation-letter PDF and sequential return-visit popup.
>
> **▶ OPEN / NEXT TURN:**
> 1. **NEEDS INPUT — Homecoming registration URLs.** Sara is expected to provide separate Blackbaud links for the free-but-required **Welcome Reception RSVP** and the **Athletic Hall of Fame banquet RSVP**. When they arrive, turn the flyer's two inactive RSVP pills and the site's inactive banquet RSVP control into real links; keep the full-weekend flyer route as the registration hub.
> 2. **NEEDS INPUT — Mark Parkinson's online store URL.** The merchandise promotion stays off the popup and Events card, but is restored on the flyer with an inactive **Lyndon Store** pill. When Mark provides the URL, turn that flyer pill into a real link using the verified destination.
> 3. **2026 HOF inductee photos + bios — PENDING (expected late Aug / after the Sept induction ceremony).** The Class of 2026 panel in the HOF box currently shows **names + grad years only** (Wildens Fils '15 · David Cady '94 · David Chase '85 · Anthony Moccia, honorary/in memoriam) with the note "Photos and full bios will be shared as they become available." When Steve / VTSU (Sara Lussier) send headshots + bios, swap the note for real content.
> 4. **Aldrich House section — LIVE with the redacted PDF linked + a sequential return-visit popup; still PENDING = the house photos.** Michael's letter references attached photos (house, front door, corner pilasters, newel post, radiator) but they are **NOT in the PDF** (the only image in it is the MVS logo) — need Michael/Alex to send the actual image files, then place them in the section (currently text-forward) and ideally swap the popup's placeholder image (currently the Manor Vail postcard).
>
> **Locked decisions (don't relitigate):** Spotlight is a **flat 3×3**, not themed triads (verified pool too meteorology-heavy — 7 mets, 1 business, 0 athletes). Mayo '90 & Priestley '09 badges are **Alex-approved ESTIMATES** (no public grad year). Benning shown as alumnus/judge only (he's LSC **Foundation** president — Foundation never tied to the donate flow). Mark Hilton excluded (2026 Distinguished Alumni role unverifiable publicly). Donate CTA copy/URL is sacrosanct — never "Give Now"/VSU-branded.
>
> **Env / gotchas:** Repo **syncs across machines** (Google Drive + Alex's **Command Center** dashboard, which now iframes the site via a `_headers` CSP `frame-ancestors` change) — history can interleave/rebase between sessions; consider `git pull` before editing. **Pushing works from the sandbox** (cached GitHub creds; git progress prints **red to stderr but isn't an error** — confirm via the `old..new main -> main` line). If Git Bash throws a cygwin **"add_item ... fatal error"**, just retry. Repeated headless Chrome PDF/PNG exports can corrupt command/cmdlet lookup in the reused PowerShell terminal; use a **fresh terminal** or explicit executables (`C:\Progra~1\Git\cmd\git.exe`, `C:\Users\27698\AppData\Local\Programs\Python\Python311\python.exe`). For exports, use an isolated `--user-data-dir` and launch Chrome via `Start-Process -Wait`; invoking Chrome directly with `&` can return before files finish writing and leave stale artifacts in place. The in-app screenshot tool can crop/hang on letter canvases — trust DOM overflow checks plus the true 816×1056 exported PNG. Press-photo hotlinks (Valade @ Caledonian Record, Fitzgibbon @ Future plc CDN, Cantore/Gregory station photos) — confirm reuse rights before relying long-term; all keep `onerror` fallbacks.
>
> *(QC 2026-06-04 — `docs/QC-HANDOFF.md`. Analytics: Alex set up Cloudflare Web Analytics himself. Spotlight run notes: `fundraising/SPOTLIGHT-PLAN.md`.)*

## What this is

Static single-page site for the VTSU Lyndon Alumni Council (Lyndon Teachers College → Lyndon State College → NVU Lyndon → VTSU Lyndon, since 1911). Deployed via Cloudflare Pages (git-connected to the repo), **live at https://lyndon-alumni.pages.dev**. Source: `index.html` at root, no build step.

- **Upstream mirror:** https://github.com/almaster1987/lyndon-alumni
- **Owner / contact:** Alex Lataille (Alexander.Lataille@icf.com), member of the Council's Fundraising Committee.
- **Hosting accounts:** this project runs under Alex's *personal* Google identity — GitHub `almaster1987`, Cloudflare + Gmail `alexander.lataille@gmail.com` (NOT the ICF email). Council correspondence (e.g. Michael Thurston / Manor Vail Society, michaelvt2700@gmail.com) also goes via that Gmail.
- **Council president:** Steve Cormier (LSC '82).

## Donate flow — read carefully before touching the Give section

The donate CTA points to this Blackbaud Renxt form:

```
https://host.nxt.blackbaud.com/donor-form/?svcid=renxt&formId=fed909dd-635d-47ad-9868-24becf560417&envid=p-BzoGXjMynEG3LMXBAdBMug&zone=usa
```

Gifts go to the **Lyndon Alumni Association** directly — *not* to VSU/VTSU and *not* to the Lyndon State College Foundation.

### Rules (do not violate without asking)

1. **CTA copy:** "Donate to the Association" or "Donate to the Alumni Association." Never "Give Now," "Give to Lyndon," or anything VSU/VTSU-branded. Reviewer feedback in April 2026 was that "Give Now" looked like donating to the school.
2. **CTA styling:** prominent gold (not standard green). Pulsing in nav, gradient button on the donate card.
3. **No dollar-amount selector** on the donate card — the Blackbaud form collects amount on the next step. A selector here is redundant and was removed.
4. **The Lyndon State College Foundation is NOT the processor.** It's a separate 501(c)(3). Do not name it as the processor anywhere on the site. A plain "see also" link in the footer Resources block is fine — keep it disconnected from the donate flow.
5. **Foundation link** (https://www.lyndonstatecollegefoundation.com) lives in the footer Resources block only.

## Deploy

**Live host: Cloudflare Pages**, git-connected to the upstream repo. Every push to `main` auto-deploys — including via the one-click `push-to-github.bat`. No build step (build command: *none*, output dir: `/`). Security + cache headers come from the `_headers` file — **Cloudflare ignores `netlify.toml`**.

GitHub Pages was the prior host; it was **disabled on 2026-05-31** (Pages source set to *None*), so `lyndon-alumni.pages.dev` is the **sole** live URL — the old `almaster1987.github.io/lyndon-alumni` now 404s. (Source = None also stops pushes from silently re-publishing it.) `netlify.toml` is a dormant alternative config, not the live host.

**Pushing:** As of **2026-07-02**, `git push` **works from the sandbox** — GitHub credentials are cached (Git Credential Manager), so Claude can commit *and* push, and Cloudflare auto-deploys. Heads-up: git writes progress to **stderr**, so PowerShell renders it as red "RemoteException" text — that is **not an error**; confirm success by the `old..new  main -> main` line (and/or `git status -sb` showing in sync with `origin/main`). If a push ever hangs on a credential prompt, fall back to Alex running `git push` himself. (Prior note: pushing used to fail on a credential prompt — no longer the case.)

Local preview: open `index.html` in a browser, or `python -m http.server 8000`.

## Folder structure

```
Lyndon/
├── index.html            # the site
├── _headers              # Cloudflare Pages security + cache headers (LIVE config)
├── netlify.toml          # dormant Netlify config — NOT the live host
├── push-to-github.bat    # one-click commit + push (auto-deploys to Cloudflare)
├── README.md             # human-facing readme
├── CLAUDE.md             # this file — Claude Code auto-loads it
├── .gitignore
├── docs/
│   ├── PROJECT_CONTEXT.md   # full project background
│   ├── DONATE_FLOW.md       # donate-specific rules + URL
│   └── SETUP.md             # onboarding on a new machine
├── fundraising/          # Council-internal (gitignored — never deployed)
└── lyndon-alumni-original/  # read-only reference clone (gitignored)
```

`lyndon-alumni-original/` is the cloned upstream repo for reference — **do not edit it**, and do not include it in deploys.

## When Alex asks for a change

- He's writing from inside the Council (Fundraising Committee), not as an outsider asking for background.
- Practical, action-oriented suggestions. He has authority on Council decisions but coordinates with John Kleinhans (Fundraising chair) and Steve Cormier (president).
- After making donate-related edits, double-check the URL hasn't drifted from the one above.

## Recent changes worth remembering

- **2026-08-05 (red registration actions + flyer store placeholder — pushed live):** Changed Homecoming registration actions on the popup, Events section, golf card, Hall of Fame box, and detailed flyer from green/gold to the existing rust-red brand treatment so they stand out from general navigation. Replaced the flyer heading's “All events are in Lyndonville unless noted” line with the compact **Lyndon gear: Available Saturday during the games** callout and an intentionally inactive green **Lyndon Store** pill. The merchandise promotion remains absent from the popup and Events card. Regenerated only the flyer PDF/PNG. **Validation:** no source diagnostics; phone-width web buttons have white text and no local overflow; flyer is a one-page 8.5×11 PDF with an 816×1056 PNG; gear/store controls are visibly rendered; QR decodes to the hosted flyer route at 144/192/300 DPI.
- **2026-08-05 (Homecoming registration hierarchy — pushed live):** Removed the Saturday Lyndon-gear promotion from the first-visit popup, Events card, and detailed flyer. Reworked the Homecoming Events actions into a clear two-step stack: **Register for the Full Weekend** (hosted flyer) with **Homecoming Poster** beneath it, plus copy naming golf, welcome-reception, and banquet registration. Replaced the site's gray banquet placeholder and the flyer's “coming soon” lines with deep-green **“RSVP: Let us know you're coming”** controls; they remain intentionally unlinked pending Sara's URLs. The flyer golf pill links to the live Blackbaud registration form. Removed the Sunday “Make a weekend of it” box; changed the flyer footer to list all three registration paths; and pointed both flyer/poster QRs to `https://lyndonalumni.com/homecoming-2026`. Regenerated both PDF/PNG pairs using isolated, explicitly waited Chrome processes. **Validation:** source diagnostics and diff checks clear; web controls have no local overflow at desktop/phone widths; PDFs are one-page US Letter; PNGs are 816×1056; both PDF QRs decode to the exact flyer route at 144/192/300 DPI.
- **2026-08-05 (final detailed-flyer hierarchy + custom-domain QR — pushed live):** Consolidated the merchandise message into a compact **Lyndon gear** callout beside the Weekend Lineup heading (available Saturday during the games; online shop link still coming soon), folded the free-breakfast/RSVP status into the reception details, removed the redundant Friday golf-registration note, and gave Sunday's return-home note a restrained rust accent. The footer's visible URL and framed QR now both target **`https://lyndonalumni.com/#events`**. Regenerated the flyer PDF/PNG; validation passed with no editor or diff diagnostics, an 816×1056 PNG, a one-page 8.5×11 PDF, and an exact QR decode to the custom-domain Events anchor. The main site and hallway poster were unchanged in this deployment.
- **2026-08-04 (detailed-flyer QR correction — validated, ready to push):** Changed the bottom-right flyer QR and visible URL from the self-referential `/homecoming-2026` PDF route to **`https://lyndon-alumni.pages.dev/#events`**. Fixed the clipped gold frame by reducing the QR/grid column from 1in to **.88in** and explicitly centering it; rendered geometry = 84×84px code box inside a ~103px footer, with **6.11px top / 6.13px bottom frame clearance** and a 0.01px vertical-center delta. Regenerated the flyer PDF/PNG via local Chrome. **Validation:** HTML diagnostics clear; no Letter-sheet overflow; visual export shows the complete centered frame; PNG = 816×1056 and decodes to the exact Events URL; PDF = one 8.5×11 page and decodes at 144/192/300 DPI from multiple footer crops. The legacy VS Code validator's hard-coded narrow flyer crop is still not authoritative for the moved/framed code.
- **2026-08-04 (Homecoming call staging — validated, ready to push):** Based on Alex + Steve's catch-up, synchronized the web and print surfaces around the **Welcome Reception for President Sherry Kollmann** (Saturday, 9:30–11:30 AM): corrected the site spelling, replaced the old summary, and retained the Class of 1976 / Q&A context. Added a compact **Shop Lyndon gear** callout to the Homecoming popup/card and flyer: **Mark Parkinson '98** will sell Lyndon State College and NVU Lyndon merchandise at the soccer field, **Saturday noon–5 PM**. Added native disabled site controls for the pending **Breakfast RSVP** and **Banquet Registration** URLs; the flyer plainly says "RSVP link coming soon" and "Registration link coming soon" rather than inventing destinations. The existing local poster revision uses the matching Welcome Reception language. **Validation:** editor diagnostics clear; the changed desktop/mobile card surfaces have no local overflow; flyer and poster PDFs are one-page US Letter, PNGs are 816×1056, and both QR destinations decode to `https://lyndonalumni.com/homecoming-2026` from their complete footer regions. **Export gotcha:** the VS Code task `Final Homecoming artifact validation` uses an overly narrow hard-coded flyer crop which cuts into the now-framed code and reports a false negative; validate the complete footer QR crop instead. The old `_export-homecoming.ps1` is absent; local Chrome headless export plus an 864×1104 full-canvas screenshot cropped 24px on each edge produced the final flyer assets.
- **2026-07-28 (Homecoming collateral controls temporarily disabled — pushed live, commit `f159a2b`):** At the team's request, replaced the Homecoming card's **Full Weekend Schedule** and **Homecoming Poster** anchors with real disabled `<button>` controls and added a muted gray `.btn-disabled` state. The labels and layout stay in place, but the controls have no `href`, cannot receive clicks, and expose native disabled semantics; the underlying flyer/poster routes and files were deliberately left intact for reinstatement in a few weeks. **Validation passed:** no editor diagnostics; both controls are disabled and gray with no horizontal overflow at 1440px and 390px; both `lyndonalumni.com` and `lyndon-alumni.pages.dev` serve the same disabled DOM state; `/CLAUDE.md` still returns a 301 to the homepage. Deployment was allowlisted to `index.html` only, so the separate local flyer revision and this context file remained unpushed.
- **2026-07-28 (detailed flyer social emphasis — LOCAL, NOT PUSHED):** Alex asked for the Saturday **Cookout (1 PM)** and **Ice Cream Social (2 PM)** to stand out more on the detailed flyer. Added matching pale-green `.social-feature` row bands with dark-green **Free Cookout** / **Free Ice Cream Social** labels; the sports remain the primary row titles, while gold stays reserved for the three marquee events. Regenerated `Homecoming-Flyer-2026.pdf` and `.png` from the HTML; the poster and `index.html` were not touched. **Validation passed:** no editor diagnostics; both rows and labels fit their boxes; no element escapes the 816×1056 sheet; fonts and images loaded; PDF = one 612×792-point US Letter page; PNG = 816×1056; printed QR crop decodes to `https://lyndonalumni.com/homecoming-2026`. Local SHA-256 before this context edit: HTML `fa7b891fcfb60a5fe98e993e9a8966fb7627b9736b2de36cf134e6150b72b322`; PDF `ca9537b3a908f00f70d5ecf1e47b3b5d71b86cd588a275fe4387e13dd475be47`; PNG `3b75c581e959a21bbb933b1589d365a11789e8c7ea08e485056fc349ea488bc2`. Starting commit was `d8e3902`; only the three flyer files were modified before this checkpoint. **Next exact action:** review, then commit/push those three files plus `CLAUDE.md` and verify the live PDF/hash; no deploy has happened yet.
- **2026-04-27:** Rebuilt site from upstream after losing local files. Reworked donate CTA per reviewer feedback (gold pulsing nav button, "Donate to the Association" copy, gold-bordered card, removed amount selector).
- **2026-05-22:** Per Michael Thurston (Manor Vail Society - Lyndon, '74) feedback: split the 1911 timeline entry into two — 1911 "Teacher Training Course" (one-year program at LI campus) and 1920s "Lyndon Normal School" (school-within-a-school at LI after expansion). Exact year of the Normal School name change is TBD — Michael was going to dig for it. **Open ask:** add a Vail Mansion image to the header `.gallery-strip` panorama once Michael forwards photo choices from MVS. Vail Mansion was the campus 1951–mid-1960s and the centerpiece through 1974; significant to alums spanning 1947–1977. Credit any image to "Manor Vail Society - Lyndon."
- **2026-05-31:** Migrated hosting **GitHub Pages → Cloudflare Pages** (`lyndon-alumni.pages.dev`), git-connected to the repo — Cloudflare's GitHub App is scoped to **only** `lyndon-alumni`; pushes to `main` auto-deploy. Added `_headers` (security + cache; Cloudflare ignores `netlify.toml`) and gitignored `Archive/` (it held a copy of prospect data). **Disabled GitHub Pages** (source → None) so there's one canonical URL. Verified the live site matches Alex's reply email to Michael Thurston (timeline split is live; **Vail Mansion image still pending** Michael's MVS photos; the "1920s" Normal School year is still TBD). **Custom domain:** none yet — when Alex buys one, point it at Cloudflare (it'll mask the pages.dev URL). **Content updates were deferred** this session ("hold for today") — pick them up next time.
- **2026-06-04:** Integrated the **June 3 alumni-team meeting** (Steve Cormier + Meaghan Meachem; transcript in gitignored `Meeting Transcripts/`) and pushed **live to main**. Changes: (1) **Donate fund link updated** — formId `fed909dd-…` now points to the **Lyndon Alumni Council Fund** (old `0b410455-…` sometimes defaulted to the wrong fund); stripped the `bbeml=` email-tracking token Alex's link arrived with — never hardcode `bbeml`. (2) Brand is now **"Lyndon Alumni Council"** — dropped the "VTSU" prefix from the council name everywhere; spell out "Vermont State University" in body prose; "VTSU Lyndon" kept only in the Mentor card, the 2023 timeline node, the nav lineage chip, and links to the university's own VTSU properties. (3) Top banner promotes **Fall Homecoming for Alumni & Families** (Spring Homecoming dropped — it's past). (4) Photo strip is now an auto-advancing **carousel** (pauses on hover, respects reduced-motion). (5) Events: "Fall Weekend" → **"Fall Homecoming"**; Spring Homecoming card replaced by a **Golf Classic** card (**St. Johnsbury Country Club** per Steve's June Homecoming announcement email — corrected from the old site's "Orleans Country Club"; register link still "coming soon"; added a generic Wikimedia golf-course photo to the card header with a gradient fallback). **Homecoming dates confirmed Sept 25–27, 2026** (from that email) — banner, quick-link, and both event cards updated; Fall Homecoming activity list rewritten to match the email's official lineup (HOF & Alumni Awards reception, M&W soccer, alumni baseball/softball/basketball, Alumni Rugby match return, "more to come" from the poll). (6) Give section reframed — everything flows to the **one Lyndon Alumni Council Fund**; removed false "Athletics"/dedicated-scholarship earmarks (donating to the Association ≠ athletics, per Meaghan; scholarships flow through the college). (7) Get Involved trimmed **6 → 3** cards (Join → emails Steve `scormier55@comcast.net`; Mentor via Handshake; Connect-on-social). (8) Council size **15 → 20**; removed Cole Alexander from Alumni/Student. (9) Meetings "Zoom" → "virtual." (10) Removed the **News** section (returns with Comms press releases) and the dead **newsletter signup** form. (11) Contact: mailing address **1001 College Road, Lyndonville VT 05851 · Attn: Alumni Council**; Email Us → Steve. **Pending content (placeholders live):** golf register link, exact tee time & entry fee; 2026 Hall of Fame inductee names+photos; expand Alumni Spotlight to 6 (candidates: Mark Hilton, Mark Valade '78/Carhartt CEO — verify Lyndon link); real newsletter-subscribe form (routes to Kate at the college, needs name+email); dedicated **Manor Vail** section (ask Mike Thurston); confirm the 60K alumni stat (Steve→Ernie) and the committee roster. **Domain:** Alex to buy **`lyndonalumni.com`** (Meaghan gifting if <$50 via Venmo) and point it at Cloudflare. Steve OK'd sharing the site with Johnson's president. **Carousel update (same day):** the campus photo strip is now **two side-by-side carousels** with captions centered **below** each photo (JS generalized to init every `.carousel` block; staggered auto-advance; pauses on hover; reduced-motion safe). Added the two **Vail Museum aerial postcards** (campus c. 1976–77 and c. 1990s, David & Beth Kanell Collection) — hotlinked from CatalogIt's CloudFront CDN (`d8e7jbdw4fu0e.cloudfront.net`, pulled from each entry's `og:image`) with an `onerror` fallback to a campus photo, plus a footer credit line. **Confirm usage rights with Michael Thurston / MVS** before relying on these (museum-catalogued postcards). Then expanded the same turn to **three** side-by-side carousels (2-up tablet, 1-up phone) with **15 photos** (5 each): added 6 more Vail Museum/Kanell historic shots (1970 & 1980s–90s aerials, early-'60s LTC postcard, night scene, Manor Vail postcard, 1962 Burke Mtn valley view) and swapped the **About-section image to the c.1990s aerial** (credit → Vail Museum). Removed the dead **Instagram & YouTube** social placeholders (Facebook/LinkedIn/Bluesky/Handshake remain); **60K+ alumni stat confirmed OK by Alex.** For the ~30 photos Alex eventually wants, pull more from the Vail Museum CatalogIt (`hub.catalogit.app/vail-museum`; its `sitemap.xml` lists all entries — use each entry's `og:image`, `.lg@2x`/`.lg-nt@2x` renditions). **Next turn = QC — see `docs/QC-HANDOFF.md`.**
- **2026-06-04 (QC turn):** Ran the full QC checklist (`docs/QC-HANDOFF.md` has the results). Verified 40+ links + all 21 images live (HTTP status + rendered-DOM audit via a local preview), every internal `#anchor` resolves, no `#news`, one intentional `href="#"` (brand). **Site passed.** Only fix: **removed the footer "Council Dashboard" link** — it pointed at `fundraising_dashboard.html`, which isn't deployed, so Cloudflare served the homepage at 200 (soft dead-end); the real dashboard stays gitignored/undeployed (no data exposure). Confirmed `/nominate` 301s to a live "VTSU Alumni Award Nomination" Formstack form. **Two checks need a human** (can't be done headlessly): (a) click Donate and confirm the Blackbaud form shows the **Lyndon Alumni Council Fund** (URL + formId are unchanged/correct, form returns 200 — just the JS-rendered fund label is unverifiable from here); (b) confirm the **Facebook group** loads (FB login-walls bots). Pending-content placeholders from the earlier 06-04 entry are unchanged.
- **2026-06-05:** Content update from two Council docs (`Homecoming lineup 2026.docx`, `Lyndon Golf Tournament 2026.docx` — on Alex's Desktop, not in repo) + a note from the college. (1) **Hero alumni stat 60K+ → "10,000+"** — the college reported **10,344 known alumni**; Alex chose the rounded "10,000+" (supersedes the 06-04 "60K+ confirmed OK"). (2) **Golf Classic card filled in:** **Friday, Sept 25, 2026**, St. Johnsbury CC, 11am reg · **noon shotgun**, **$125/golfer** (pays online "through the college"), sponsor tiers ($2,500 presenting / $750 hole-in-one / $100 hole), longest-drive/closest-pin/putting/50-50. Register button still "Link Coming Soon" (no pay-portal URL yet). (3) **Fall Homecoming card highlights refreshed** to the official lineup (HOF & Alumni Award banquet, "Celebrate Vail"/Class-of-'76 reunion + Q&A w/ Pres. Kollman, alumni games + rugby, M&W soccer, Vail Museum open house, cookout/ice-cream/Octoberfest, "schedule firming up"); stale "Get Notified When Dates Drop" CTA → "Get Homecoming Updates." Alex chose **highlights-only** over a full day-by-day schedule (many lineup items are "tentative"). (4) **Subscribe button added** to the Contact section → VTSU alumni contact-info form (per the college: reuse their form, don't build a custom one routing to Kate). **Deliberately NOT published:** the golf flyer's internal volunteer/sponsor-prospect names (Hannah Manley, Bob McCabe, Skip & Connie Pound, etc.) and the organizer cost basis ($55/golfer, cart costs). Verified via local render: hero stats, golf card, subscribe button (opens new tab), zero broken anchors. **Pushed live to `main` 2026-06-05** (both the QC fix and this content update) and verified the Cloudflare deploy — the live site shows `10,000+`, the Subscribe button, and the golf details, with the "Council Dashboard" link gone. Note from the college: edits to *their* VTSU alumni pages go to "Leo, their website guy" (not our Council site).
- **2026-06-05 (spotlight set-up):** Phased up the next turn — **expand the Alumni Spotlight 3 → 9** via a complex multi-agent analysis. Full spec + candidate leads + guardrails live in gitignored **`fundraising/SPOTLIGHT-PLAN.md`** (kept internal because it references confidential prospect data). Public-facing decisions: **Cantore '86 stays**; include Carhartt CEO **Mark Valade '78** (corrected from the mis-noted "Valli"; **verify his Lyndon attendance from a public source** before publishing it as fact); strongly consider 2026 Distinguished Alumni **Mark Hilton** (per the June 3 transcript — speaking at graduation, at Homecoming). Likely layout = **3 themed triads** (meteorology / business / public-service-arts-athletics), i.e. the "3×3×3." **Guardrail:** feature only genuine public figures; publish names + public role only, never wealth/ask/donor-status; verify every fact publicly; keep `onerror` image fallbacks. Analysis runs next turn (Ultracode → Workflow).
- **2026-06-06 (Spotlight built & pushed live):** Ran the analysis as **two Workflows** — (1) discover (8 angles) → shortlist → adversarial verify (2 skeptics/candidate: attendance + role) → assets → synthesize; (2) a targeted gap-fill hunt for business/athletics figures. Verified pool came back **meteorology-heavy: 7 mets, 1 business (Valade), 4 public-service/arts, 0 athletes**. A balanced themed-triad 3×3×3 was therefore **impossible without padding** (would break the "genuine public figures only" guardrail), so — confirming layout with Alex first — shipped a **flat 3×3** of the 9 strongest verified Hornets (mets spread across the grid; existing 3 cards kept, 6 added). **Final 9:** Cantore '86, **Valade '78** (Carhartt Exec. Chairman — Lyndon link now PUBLICLY CONFIRMED: NVU release "Mark Valade '78", Caledonian Record "1978 alumnus of Lyndon State College", VTDigger), Joe Benning '79 (VT Asst. Judge / former State Senator), Cindy Fitzgibbon '95 (WCVB Boston, 1st female chief met in market), Matthew P. Mayo (Spur-winning author; no public grad year → no class badge), Justin Chenette '12 (Maine legislator / York Co. Commissioner), Nick Gregory '82 (Fox 5 NYC), Monique Priestley (VT State Rep, NVU-Lyndon; no grad year → no badge), André Bernier '81 (Fox 8 Cleveland, ret.). **Mark Hilton DROPPED** — attendance confirmed but "2026 Distinguished Alumni" role unverifiable publicly (reported 2026 commencement speaker was ex-president Joe Bertolino); revisit if Steve sends an official bio/photo. **Guardrails honored:** Benning = alumnus/judge only (he's LSC Foundation president — Foundation untouched); Valade card = Carhartt role only, "Read More" → Carhartt Wikipedia (not the donation story); every card keeps its `onerror` fallback. **Verified all 9 images load + 9-card layout via local render** before pushing. Spotlight intro/Nominate button unchanged. **Pending:** confirm reuse rights on the press-photo hotlinks (Valade @ Caledonian Record CDN, Fitzgibbon @ Future plc CDN, Cantore/Gregory station photos) and swap to permissioned/Wikimedia images if needed; add Hilton (+ any verified athlete/2nd business figure) later if sources materialize.
- **2026-06-07:** Three follow-up tweaks from Alex, all pushed live. (1) **Spotlight photo crop fixed** — headshots were getting their crowns/hats clipped; `.sp-hdr` height **200px → 250px** and `object-position` **`center top` → `center 22%`** so heads sit properly (verified across portrait/landscape/scene images via local render). (2) **Nav "Sign Up" button added** — an outlined-green pill (class `.nav-signup`, secondary to the gold Donate CTA so the donate hierarchy is preserved) → the VTSU alumni contact-info form (`https://vermontstate.edu/alumni/vermont-state-alumni-contact-information-form/`, same target as the Contact-section Subscribe button). Shows in the desktop top bar + the mobile dropdown; hidden in the mobile top bar (`.nav-in .nav-signup{display:none}` ≤880px) to avoid crowding. (3) **Spotlight reordered oldest→newest by class year:** Valade '78 · Benning '79 · Bernier '81 · Gregory '82 · Cantore '86 · Mayo '90 · Fitzgibbon '95 · Priestley '09 · Chenette '12. (4) **Mayo & Priestley got class badges** — ⚠️ these two years (**Mayo '90, Priestley '09**) are **ALEX-APPROVED ESTIMATES, NOT publicly verified** (no public source states either grad year; Mayo's ~1968 birth → ~'90, Priestley left for grad school in 2010 → ~'09). If exact years surface (Council/alumni records), correct the badges. All other 7 years are publicly verified.
- **2026-06-08:** Two asks from the alumni meeting, both pushed live. (1) **Online/distance students welcomed** — per the meeting ("this is their home too"), the About mission paragraph now opens with "Whether you earned your degree **on campus or online**…" and adds "Our online and distance students are every bit a part of the Hornet family, today and always." (placed in the most-read "this is your home" spot). (2) **New "Alumni Awards" section** (`#awards`) added right after the Athletic Hall of Fame box in the Events section — a `.inv-grid` of **6 cards** from Steve's council-approved criteria (source docx in `Background Files/Lyndon awards.docx`, gitignored via `*.docx`): **Outstanding Graduating Senior** (🎓), **Distinguished Alumni** (🏆), **Outstanding Young Alumni** (🌟), **Faculty Recognition** (📚, est. 1998), **Loyalty Award** (💛), **Award of Special Merit** (🎖️) — each with its criteria + the approved nomination word-count (100–150 or 75–100 words). "Submit a Nomination" buttons point to the live `https://vermontstate.edu/nominate` form (same as the Athletic HOF + Spotlight). Added a footer Events-column link "Alumni Awards → #awards". **Editorial note:** generalized the Senior award's hardcoded "May 2027 commencement" to "spring commencement" so it won't go stale; intro line folds in the online-student inclusivity. **Analytics:** Alex set up site visitor tracking himself in Cloudflare (the dashboard wouldn't load in the automated browser tab). **Domain:** `lyndonalumni.com` is now a Cloudflare zone (account `6d7ae81a…`).
- **2026-07-02:** Team feedback ahead of the college's **Alumni Award email** (going out in the next few days): make the award nomination prominent near the top instead of making people scroll to find it. Three changes, **committed `20fb52f` and pushed live** (verified the awards banner + outline card render on the pages.dev URL). (1) **Quick Links strip:** replaced the **"Join the Council"** card with an **"Alumni Award Nominations"** card (🏆, `go` gold icon) that jumps to `#awards` — the Join-the-Council content still lives in full in the Get Involved section, so nothing was lost ("for now," per Alex — easy to swap back after the award push). (2) **Second announcement banner:** added a **green** bar (`.ann.ann-award` — `var(--green)` bg, `var(--gold-md)` link) directly below the gold Homecoming banner: "🏆 Alumni Award nominations are open — honor a Hornet who's made their mark. → Nominate an Alum" linking to `#awards` (deliberately a different color from the gold Homecoming banner, per the ask). (3) **Chasing-outline highlight** on the award card: first tried a rotating conic-gradient ring (Alex: "obnoxious with the spinny thing"), so switched to a subtle **SVG stroke-dash outline** — one thin gold line that chases around the card's outside edge. Built as an SVG `<path>` (**NOT `<rect>`** — `pathLength="100"` isn't reliably honored on `<rect>` in Chromium, which rendered ~4 repeated dashes; `<path>` yields a single clean dash), with `preserveAspectRatio="none"` + `vector-effect:non-scaling-stroke` so the line stays crisp at ~2px on any card size; `@keyframes qlDash` animates `stroke-dashoffset` 0→-100 (~3s/lap). **Reduced-motion** falls back to a static faint gold outline. Verified via local `python -m http.server` render + screenshots before pushing.
- **2026-07-03:** Steve Cormier relayed the college's press release (via Meaghan Meachem / VTSU Comms) announcing new Vermont State University President **Dr. Sherry Kollmann** (began July 1, 2026) and asked to feature it prominently. Built a **dismissible popup/modal** (`#presPop`) that auto-opens ~0.9s after load over a dimmed green backdrop: her photo (left on desktop / top on mobile), a gold "News from Vermont State University" eyebrow, the headline, "For Immediate Release · July 1, 2026", and the press release condensed into a faithful **scrollable** read (Kollmann + Dickinson quotes, NMSU Global stats; added "— including Lyndon —" since this is the Lyndon site; dropped only the email-signature / DEI / privacy boilerplate). Actions: a gold **"Watch her welcome message"** button → the release's real YouTube link (`youtu.be/WorDJXBbwc0`) + a **Dismiss** button. **Four dismiss paths:** the ✕, the Dismiss button, backdrop click, and Esc. Dismissal is remembered via `localStorage` key **`lac_pres_kollmann_2026`** — once dismissed it won't reappear on that device (to re-show for a future announcement, **bump the key**). Includes focus trap + body-scroll lock + `onerror` SVG fallback + reduced-motion handling + hidden in print. **Photo:** Alex dropped `unnamed.jpg` in the repo root → renamed to **`president-kollmann.jpg`** (`.jpg` isn't gitignored, so it deploys with the site). Verified both layouts (desktop 2-col / mobile stacked), the dismiss flow, and localStorage persistence via local render + screenshots, then **committed + pushed live**. **Gotcha logged:** `localhost:8123` is hijacked by a **leftover service worker from a different local app ("Money Dashboard")** that serves its own cached app at that origin regardless of the files on disk — preview this site on a *fresh* port (used 8791) to avoid it. **Tweakables if asked:** make it re-show each visit (sessionStorage, or drop the key) instead of once-ever; or trim the full release to a teaser + "read more."
- **2026-07-03 (mobile nav fix):** Alex reported the site wasn't good on mobile (desktop was "perfect — don't change it"). Diagnosed via local render at phone widths: the site was *already* responsive (every section stacks to one column fine), but the **mobile top nav was broken** — the full-text gold "Donate to the Association" `.nav-cta` stayed in the top bar on phones, overflowed the viewport (~72px), and pushed the **hamburger off-screen** (clipped by `body{overflow-x:hidden}`), so the menu was unreachable + the page scrolled sideways. **Fix (all scoped to mobile media queries — desktop bytes-identical, verified):** (1) top-bar `.nav-cta` now shows a **compact gold pulsing "♥ Donate" pill** on mobile via two spans — `<span class="cta-lg">Donate to the Association</span><span class="cta-sm">Donate</span>` with `.cta-sm{display:none}` by default and swapped inside `@media(max-width:880px)`; the **full "Donate to the Association" copy is preserved** in the hero button, the slide-down dropdown, and the "Most needed" quick-link card (donate-copy rule honored — "Donate" alone is not "Give Now"/VSU-branded). (2) Brand scales down on small screens: `.brand-txt strong` 1rem→.9rem (≤430px)→.8rem (≤360px), and the **subtitle chip hides ≤360px** so brand + Donate + hamburger fit one line. (3) Added `html{scroll-padding-top:80px}` (mobile only) so anchor taps don't hide section headings under the 68px sticky nav. **Verified in-browser: zero horizontal overflow + fully-visible working hamburger from 360–414px+ (all standard phones), dropdown shows every link + Sign Up + full Donate CTA, desktop unchanged (full CTA text, nav links + Sign Up visible, hamburger hidden).** Committed `fa71eff` + **pushed live**. Note: CSS media queries key off **viewport width**, but the Playwright emulator's clientWidth is ~15px narrower (fake scrollbar) — real overlay-scrollbar phones have *more* room than the emulator shows, so emulator-clean = device-clean.
- **2026-07-08:** Golf tournament finalized + a few footer/sponsor tweaks, all pushed live. (1) **Footer:** the dead "Contact" link (pointed at `#contact`) was replaced with **"Designed & deployed by Alexander Lataille (class of 2010)"** → `mailto:alexander.lataille@gmail.com`. (2) **Golf sponsor:** added a green callout on the golf card — "🏆 **Anton LeMieux Financial Group** — our Hornet Overall Champion Sponsor." (3) **Golf tournament — Sara Lussier's edits (via Steve), registration now LIVE:** RENAMED **"Lyndon Athletics Golf Classic" → "The Skip Pound and Dudley Bell Legacy Golf Classic"** (to match the registration form) in the card title + blurb; **proceeds** line (both the blurb and the acts list) now read "support the Lyndon Alumni Council **and Lyndon Athletics**"; **sponsor tiers** updated to **$2,500 Hornet Overall Champion · $1,500 Hornet Green · $1,000 Hornet Hole-in-One · $500 Hornet Spirit · $150 Hornet Hole**; and the **Register button is now LIVE** — links to the Blackbaud **registration/pay form** (`registration-form/?formId=5cb5857c-a592-42b9-84e0-f9be07180f4b`, same `envId` as the donate form) in a new tab, replacing "Register — Link Coming Soon" (fee line → "$125 per golfer — register & pay online"). **Timing:** the earlier email said the form opens Mon **July 13**, so a persistent local scheduled task (`lyndon-golf-registration-golive`, in `C:\Users\27698\.claude\scheduled-tasks\`) had been set to auto-publish it that day — but Steve then said "it's go time," so it was **activated early (today) and that scheduled task was CANCELLED/disabled.** ⚠️ Activated before independently confirming the Blackbaud form is accepting registrations (it's a JS SPA — can't verify open/closed via fetch); Alex to do a quick test-click. (4) **External change noted (synced in from another machine):** a commit "Allow framing by Command Center dashboard" changed `_headers` from `X-Frame-Options: DENY` to `Content-Security-Policy: frame-ancestors 'self' https://lataille-command-center.pages.dev` so Alex's **Command Center** dashboard can iframe the site. **The repo now syncs across machines (Google Drive + the Command Center), so history can interleave/rebase between sessions** — a push here showed a reordered base once; verified no work was lost. Consider `git pull` before starting edits.
- **2026-07-08 (correction — golf registration back to Monday):** Steve clarified he'd rather the registration **go live Monday July 13**, not early ("to be safe"). So the golf **Register button was reverted to "Register — Link Coming Soon"** and the fee line back to "$125 per golfer — pay online through the college," and the `lyndon-golf-registration-golive` scheduled task was **re-armed for Mon 2026-07-13 ~7:12am ET** (it flips the button to the live Blackbaud registration/pay link + fee line → "register & pay online"). **Sara's content edits stay live** (new name "The Skip Pound and Dudley Bell Legacy Golf Classic," proceeds → "Lyndon Alumni Council and Lyndon Athletics," updated sponsor tiers, Anton LeMieux Champion-Sponsor callout). Alex **test-clicked the Blackbaud form and confirmed it works**, so Monday's auto-publish is safe (backstop: a manual "push the golf registration" also works).
- **2026-07-13:** Golf Classic registration went live — the `lyndon-golf-registration-golive` scheduled task fired as planned and flipped the Register control from the "Register — Link Coming Soon" placeholder to an active filled-green **"Register Now →"** button linking (new tab) to the Blackbaud registration/payment form (`registration-form/?formId=5cb5857c-a592-42b9-84e0-f9be07180f4b`, same `envId` as the donate form); the fee line changed from "pay online through the college" → "register &amp; pay online." Committed + pushed to `main`, Cloudflare auto-deployed, and verified live on lyndon-alumni.pages.dev (page contains the registration-form URL + "Register Now", "Link Coming Soon" gone). Donate CTA copy/URL untouched — this change only touched the golf Register button and its fee line.
- **2026-07-20 (2026 HOF class posted):** Per Steve Cormier's ask ("Can we post this"), added the **2026 Athletic Hall of Fame induction class** to the HOF box in the Events section — committed `b2fd381` + **pushed live** (verified on the pages.dev URL). Added a gold-accented **"🏆 Meet the Class of 2026"** panel below the HOF header/buttons with a responsive 4-card grid (`.hof26-grid`, 4-up desktop → 2-up ≤700px → 1-up ≤420px): **Wildens Fils (Class of 2015) · David Cady (1994) · David Chase (1985) · Anthony Moccia (Honorary • In Memoriam)**. Names/years are authoritative — straight from the HOF committee's own recap (Cam Curler + Steve's forward), so no external verification needed; **Moccia is honorary + posthumous** so he carries no grad-year badge. **Per Sara Lussier (VTSU): names + grad years only for now — bios post after the induction ceremony, photos come ~late August**, so the panel carries the note "Photos and full bios will be shared as they become available ahead of this fall's induction ceremony." The old "2024 Inductees" italic line was moved into the new panel as a "Recent inductees — 2024:" footnote (nothing lost). **Still pending** (was the "2026 HOF names+photos" placeholder — names half now DONE): inductee **photos + bios**, expected late Aug / after the Sept ceremony — swap the note for real headshots when Steve/VTSU send them.
- **2026-07-20 (internal docs no longer public — `_redirects` edge block):** Found that the **tracked context files were being served on the live site** — `/CLAUDE.md`, `/docs/PROJECT_CONTEXT.md`, `/docs/DONATE_FLOW.md`, `/docs/QC-HANDOFF.md` (and `/README.md`) returned **200 + real content**. Root cause: git-connected Cloudflare Pages serves the whole deployed tree, and `.gitignore` deliberately keeps these tracked "for portable context" — which also web-published them. This exposed internal notes incl. the golf volunteer/prospect names + the `$55/golfer` cost basis the site **deliberately withheld**, personal emails, and a Cloudflare account-ID fragment. **The confidential `fundraising/`, `Archive/`, `Meeting Transcripts/` were NOT exposed** (gitignored → never deployed; they return the SPA homepage fallback, `text/html`, not the file). **Fix — Alex chose "block at the edge, keep tracked" (option 2):** added a **`_redirects`** file with `/CLAUDE.md / 301` and `/docs/* / 301`. Per Cloudflare's docs, *"Redirects are always followed, regardless of whether or not an asset matches the incoming request"* — so the assets still deploy (portable) but their bytes are never served (they 301 to the homepage). `_redirects` can't emit a raw **404** (Pages doesn't support `_redirects` *rewrites*, only redirects/200-proxies), hence 301-to-home. **⚠️ Do NOT delete `_redirects`** — that would re-expose the docs. **Verified live:** the four doc paths now return **301** (were `200`/`text/markdown`); following `/CLAUDE.md` lands on the homepage (`text/html`) with **no** internal content (`Project context` and `Hannah Manley` both absent); `/` + `/president-kollmann.jpg` still **200**. Committed `33211b7` (the `_redirects`) then this CLAUDE.md log. **Still fetchable but low-sensitivity plumbing (left as-is):** `/README.md`, `/netlify.toml`, `/push-to-github.bat`, `/.gitignore` — add them to `_redirects` for belt-and-suspenders if desired.
- **2026-07-20 (Aldrich House / MVS preservation section — built & pushed live, commit `82ff0db`):** Per Steve + Alex, added a dedicated **`#aldrich`** section (right after the history timeline `tl-sec`, before Events) as a supportive "advertisement" for **MVS-Lyndon's** effort to win **VT historic-preservation recognition for the Aldrich House (aka the Alumni House)** — the last original building from T.N. Vail's estate, now empty w/ heat & water disconnected & facing demolition. Source = **`MVS-Lyndon Aldrich Preservation Letter.pdf`** (Michael Thurston's June 24 2026 letter to the VT SHPO) — the section copy is drawn straight from it (Vail/Speedwell Farms history, 1974 mansion demolition, Greek Revival cottage details, the preservation petition). **Two Alex decisions (locked):** (1) **framing = "support & amplify," NOT a formal endorsement** — MVS *asked* the Council to endorse but it hasn't formally voted, so the copy reads "The Alumni Council is proud to help spread the word" (do NOT upgrade to a formal-endorsement claim without Alex/Steve confirming a real vote); (2) **PDF stays private** — "Learn more" links to the existing vermontstate.edu **Manor Vail Society** page only; the letter PDF is **deliberately NOT committed** (still untracked at repo root). Built text-forward (no unverified photo of the house), gold-left-accent card using existing classes/vars (`.sec`, `.tc`, `.lbl`, `.ttl`, `.btn-og`, `--gold`). **Deploy:** staged **`index.html` only** (allowlist — never `git add -A`, to keep the PDF out), committed + pushed to `main`, Cloudflare auto-deployed. **Verified live:** section present on pages.dev ~30s post-push; **PDF leak check passed** — `/MVS-Lyndon%20Aldrich%20Preservation%20Letter.pdf` returns the homepage SPA fallback (identical 95,403 bytes, `text/html`), i.e. the letter is NOT published. Optional follow-ups noted in the banner: a footer/Quick-Links jump to `#aldrich`, and hosting the PDF if Michael clears it. Contact: Michael Thurston (MVS-Lyndon '74), michaelvt2700@gmail.com.
- **2026-07-21 (Aldrich section temporarily removed — commit `9a7963b`):** At Alex's request, **removed the `#aldrich` section from the live site** — "they like it and we will add it back later." Purely a temporary takedown; no decisions reversed. The full section markup lives in commit `82ff0db` (and the 07-20 log entry above), so re-adding = re-insert that block after the timeline `tl-sec`. Deploy was allowlisted (`index.html` only; PDF still untracked/private). Verified live ~15s post-push: "Help Save the Aldrich House" no longer served. Also floated (not built): turning the MVS content into a **second sequential popup** gated on the Kollmann popup already being dismissed (separate `localStorage` key, shown via `else if` on load) — see banner open item #2.
- **2026-07-21 (Aldrich section RE-ADDED + redacted letter PDF published — commits `89709bb`, `c7db34b`):** Alex reversed the takedown ("they said go for it") and asked to **link the PDF and use the photos**. (1) **Section restored** — re-inserted the exact `#aldrich` block after the timeline `tl-sec`, pushed live (`89709bb`). (2) **PDF now linked, but a REDACTED copy** — ⚠️ the original letter's letterhead carries **Michael Thurston's full home address** (Barre, VT), so publishing the raw PDF would expose a private individual's home address on a public, indexable site. Alex chose **"link a redacted copy."** Produced `aldrich-preservation-letter.pdf` with PyMuPDF `add_redact_annot`+`apply_redactions` (true text removal, not a black box) stripping the street + city/state/zip; **his name/role + the full letter body stay intact** (verified visually via a rendered PNG *and* by re-extracting text). Linked it as the **primary green CTA** in the section ("📄 Read the preservation letter (PDF)"), MVS page demoted to secondary. **Committed the redacted PDF only; the original `MVS-Lyndon Aldrich Preservation Letter.pdf` is now GITIGNORED** so it can never be committed. **This SUPERSEDES the 07-20 "PDF stays private" locked decision** — a redacted letter is now intentionally public; the *unredacted* original stays private. **Verified live:** section links the PDF; redacted PDF serves as real `application/pdf` (490,016 bytes); downloaded the live copy and confirmed "Silver Circle"/"05641" absent, body intact; the **original filename path still returns the homepage fallback** (not published). (3) **Photos STILL PENDING** — not in the PDF (only the MVS logo is); need Michael/Alex to send the actual image files. Endorsement framing kept at **"support & amplify"** (Alex's "go for it" was read as approving the re-add + PDF, not a formal Council endorsement vote — do NOT upgrade the wording without an explicit confirmation that the Council voted).
- **2026-07-21 (sequential MVS/Aldrich popup — built & pushed live, commit `84b4b2b`):** Added a **second dismissible popup (`#mvsPop`)** for the Aldrich cause, reusing the existing `.pres-*` modal CSS (so responsive / reduced-motion / print-hidden come free). Content = condensed section copy; primary CTA links the redacted preservation-letter PDF; media image = the Manor Vail postcard (already on-site, w/ SVG `onerror` fallback). **Rewrote the single-popup IIFE into a generic sequential controller:** an ordered `SEQUENCE` `[{presPop, lac_pres_kollmann_2026},{mvsPop, lac_mvs_aldrich_2026}]`; on each load it opens **only the FIRST popup whose `localStorage` key is unset**, then `break`s — so selection is decided once at load time and dismissing the shown popup never chain-opens the next in the same visit. Behavior: **first-time visitor → president popup only; after it's dismissed, the NEXT visit → MVS popup; once that's dismissed too → none.** Each popup keeps its own key so this is per-browser/device (same limitation as the original popup). **Verified on the LIVE site** (real JS context) all three states: fresh→president, dismiss+reload→MVS opens & its dismiss writes `lac_mvs_aldrich_2026`, both-dismissed→nothing; plus a synchronous truth-table run of the selection logic (all 4 states correct) and zero console errors. ⚠️ **Preview gotcha reconfirmed:** the local `file://` copy renders in the Browser pane as a **static snapshot** (JS/reload/timers don't run faithfully) and a **non-foregrounded automation tab freezes rAF + CSS-transition opacity at 0** — so popup *visibility* can't be judged from the pane; test popup JS on the deployed https URL instead (the `.show` opacity transition animates fine for real users — identical code to the long-live Kollmann popup). To re-show either popup after a content change, **bump its key** (e.g. `lac_mvs_aldrich_2027`).
- **2026-07-23 (golf "Become a Sponsor" button — pushed live, commit `84ab6b7`):** Per Steve/Alex, added a **sponsor CTA on the golf card**, beside "Register Now" (both wrapped in a `flex-wrap` row so they stack on mobile). Links the Blackbaud **sponsorship** registration form (`registration-form/?formId=94a6db4a-bd0b-46d2-8eb8-330b4a311ee3`, same `envId` as the register + donate forms). Styled **`.btn-og` (outlined green)** so registration stays the primary action and it stays clear of the gold Donate CTA. Natural spot — the sponsorship tiers ($2,500→$150) are already listed right above it in the same card. Verified live on pages.dev.
- **2026-07-27 (Homecoming poster + hosted detailed schedule — pushed live, commit `0cc7b00`):** Revised the letter-size **Homecoming poster** to use its open space: enlarged the two-line HOME / COMING title, moved **SEPT 25–27** directly beneath the title/pond area, expanded all four highlight buckets with real times/details, and replaced the ambiguous sports `L` with a soccer-ball symbol. The poster's “Scan. Plan. Come home.” QR now targets the stable URL **`https://lyndonalumni.com/homecoming-2026`**. Published the existing detailed Friday–Sunday schedule flyer as tracked **HTML + PDF + PNG** (`Homecoming-Flyer-2026.*`) and added `_redirects` rules so `/homecoming-2026` (with or without trailing slash) 302s to the hosted PDF; the main Homecoming card now exposes **“Full Weekend Schedule”** beside “Get Homecoming Updates.” Poster source/exports are likewise tracked as `Homecoming-Poster-2026.*`. **Validation:** both QRs decoded to the intended route via OpenCV; both PDFs are exactly one 8.5×11 page; both PNGs are 816×1056; editor diagnostics and layout overflow checks passed. **Live verification on both domains:** route resolves as `application/pdf`, downloaded PDF SHA-256 matches the local file byte-for-byte, schedule button is present, and `/CLAUDE.md` still redirects safely to the homepage.
- **2026-07-27 (print-readability revision — pushed live, commit `abce302`):** Kept the collateral as **two distinct letter-size artifacts**. The detailed schedule now uses an asymmetric layout: Saturday owns the full-height main column, while shorter Friday/Sunday cards stack beside it; all typography was enlarged, Saturday's rows distribute through the available height, “Special guest all weekend” was simplified to **“Special guest,”** and the registration/QR band sits inside a **0.2-inch printer-safe bottom margin**. The separate hallway poster preserves the pond-led visual, with larger type throughout, **25–27 stacked directly beneath SEPT**, and President Sherry's photo/feature enlarged substantially. Regenerated all PDF/PNG exports. **Validation:** no HTML/card/footer overflow; both PDFs are one 8.5×11 page; PNGs are 816×1056; both printed QR crops decode to `https://lyndonalumni.com/homecoming-2026`; cache-busted live copies on both domains SHA-256 match the local files.
- **2026-07-27 (final hallway-poster content refinement — pushed live, commit `700e0eb`):** Moved **2026 / Lyndonville, Vermont** beneath `25–27`; changed the reunion highlight to singular **“Class Reunion”**; removed the `HOF` abbreviation and wrote **“Athletic Hall of Fame”** in full. Reframed the four highlights as the actual weekend sequence: **Friday Golf + Social** (11am check-in, noon shotgun, 7pm social) → **50th Class Reunion** (Saturday breakfast/Q&A) → **Afternoon Games** → **Awards Banquet** (Saturday evening, 6–9pm). Regenerated only the poster PDF/PNG. **Validation:** zero HTML/date/highlight overflow; one 8.5×11 PDF; 816×1056 PNG; printed QR decodes to the stable Homecoming route; both live domains serve the identical 1,700,493-byte PDF with SHA-256 `3b87f5e7b22f397efbb50cf2fca8d0c584b0d322a11002734e6957a2a3af1406`.
- **2026-07-27 (final QR + flyer hierarchy pass — pushed live, commits `35fa6bc`, `8526152`, `d83303c`, `5fe6b29`, `da36eb8`):** Mirrored the detailed flyer so **Friday/Sunday stack on the left** and **Saturday owns the full-height right column**; gave all three marquee moments — the noon Skip Pound & Dudley Bell Legacy Golf Classic, Saturday breakfast/Q&A, and the 6–9 PM Athletic Hall of Fame & Alumni Awards Banquet — matching gold highlight bars; inset the Saturday marquee times 16px from the box edge; kept 18.6px clearance below the breakfast callout; switched the footer URL to bold Inter; and enlarged/framed the flyer QR. On the hallway poster, increased the QR from `.78in` to **`1.18in` square (2.3× the area)** and enlarged the “Scan. Plan. Come home.” action band; separated Friday's Golf Tournament from its **7 PM evening social**; made **Breakfast + Q&A** the second highlight with **50th Class Reunion • Vail Museum** secondary; changed games to **Saturday + Sunday**; used four visual icons (**golf flag / coffee / soccer / trophy**); used **Saturday • 6–9 PM** for the banquet; and split the scan prompt into “Scan for the full schedule” / “Golf registration.” Regenerated both PDF/PNG pairs. The main Homecoming card now has exactly two actions: **“Full Weekend Schedule”** and **“Homecoming Poster”**; they share one row on desktop and stack cleanly on narrow screens. **Validation:** no page/card/callout/action/footer overflow; both PDFs remain one 8.5×11 page; both printed QR crops decode to the stable Homecoming route. Live copies on **both domains** match local SHA-256 exactly (flyer `f12d65c03bfcf16c74501abeafe4ffa486fca796325b09af45415017f99bd1bb`; poster `9663dcde664b999bf4b8b67024c7a696e3b9815eeefed4bc6afc76e19368a157`); both live flyer marquee rows have matching gold backgrounds/4px bars and 16px time insets, all four poster icons are present, the stable `/homecoming-2026` route serves the final flyer, and `/CLAUDE.md` remains redirected home.
