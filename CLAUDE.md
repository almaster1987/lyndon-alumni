# Project context — VTSU Lyndon Alumni Council site

> Auto-loaded by Claude Code. If you're a future Claude session, read this before editing.
>
> **▶ HANDOFF — current as of 2026-09-14.** ⚠️ **HOMECOMING IS SEPT 25–27, 2026 — ~11 days out. Treat every artifact as live production.** (Per-session narratives live in *Recent changes worth remembering* below — this banner is current state only, edited in place. Don't stack a new dated banner on top of it.)
>
> **Where things stand.** Site, flyer and landscape poster are live and verified on both domains. Per Steve's "close that access down" email, **reception + banquet registration are closed on all collateral** (`2f26d46`, `0938f01`): 5 site controls became disabled `.btn-closed` buttons — banquet ×3 (popup, Events card, HOF box) and reception RSVP ×2 — kept as visible disabled states rather than deleted, so the closure reads as deliberate; flyer pills → plain "RSVP closed" / "Registration closed" (flyer links 5→3); landscape poster markers → muted `.req-tag.closed` (gold stays reserved for live actions). **Golf registration, golf sponsorship and the donate CTA were deliberately left untouched and are still open** — Steve named only the reception and banquet.
>
> ⚠️ **THE CLOSURE IS ONLY HALF DONE AND STEVE MAY NOT REALIZE IT: the two Blackbaud FORMS ARE STILL OPEN.** All that changed is that the site, flyer and landscape poster no longer link to or advertise them — anyone holding the URL (old email, bookmark, saved flyer PDF) can still register. **Only Sara Lussier / VTSU can actually close the forms in Blackbaud** (reception `86c3c1e3-b17b-4ad2-b893-745eb5531d56`, banquet `26a9f053-4072-4241-9aac-d218a10d999b`). Confirm that happened before telling anyone registration is closed.
>
> ⚠️ **The portrait poster was NOT updated** — printed copies are already out, and it still reads "Registration required". It is also the lone artifact whose QR still targets `/homecoming-2026`; the flyer and landscape poster now open `/#homecoming` (see open item 2a).
>
> **Standing content rules (stable — don't redo, don't relitigate):** Homecoming `#events` sits above `#about` (⚠️ still **not literally above the fold** — see open item 3). Register/RSVP split per Steve: paid = **Register** (golf, banquet), free = **RSVP** (morning reception). The `Registration required` marker rule = **it appears only where there is NO clickable control** (so: both posters + print flyer; NOT the live flyer's golf/banquet rows). The Events card carries **5 controls** ending with `Homecoming Poster`, and the popup mirrors it; the big full-width `See the Whole Weekend Schedule` (`.btn-wide`) → `/homecoming-2026`. QRs on the flyer, landscape poster and print flyer all decode to **`https://lyndonalumni.com/#homecoming`**, and `index.html`'s popup controller opens **immediately** on that hash (no localStorage gate, no 900ms delay); `/homecoming-2026` still resolves to the flyer PDF so already-printed collateral keeps working. Lyndon Store pill is live + gold, **flyer-only**, not on `index.html` — ⚠️ the URL Meaghan supplied carried a Google Forms **edit-your-response token** that would have let any scanner edit a submitted order; the published form is the cleaned canonical `https://docs.google.com/forms/d/e/1FAIpQLSdlFQtAVRFIfM1RbFt1K16PYmzT0sYAyOW8zcEcgIBrtvBcdg/viewform` (same class as the `bbeml=` token stripped in June — **never hardcode either**). Vendor sells LSC/NVU gear, **not VTSU** (no license). **MEISA** = Music Entertainment Industry Student Association (Meaghan-confirmed). Flyer check-in block: Theater Lobby · Fri 6–7:30 PM · Sat from 8:30 AM.
>
> **Event facts / live forms (unchanged):** golf/reception/banquet all link to live Blackbaud forms — shared `envId=p-BzoGXjMynEG3LMXBAdBMug`; golf `formId=5cb5857c-a592-42b9-84e0-f9be07180f4b`, golf sponsorship `94a6db4a-bd0b-46d2-8eb8-330b4a311ee3`, reception `86c3c1e3-b17b-4ad2-b893-745eb5531d56`, banquet `26a9f053-4072-4241-9aac-d218a10d999b`, donate `fed909dd-635d-47ad-9868-24becf560417`. Live pills carry a **→ arrow** as the active-vs-pending tell. Friday is **Oktoberfest 7 PM at Veterans Park**; the Saturday 9:30–11:30 block is the **Welcome Reception for President Kollmann** (alumni, families, faculty, staff and administration, current and former; Class of 1976 50th as the secondary note). 2026 Athletic HOF class posted; internal docs blocked via `_redirects`; **Aldrich House / MVS section** (`#aldrich`) live with the redacted letter PDF and sequential return-visit popup.
>
> **▶ OPEN / NEXT TURN:**
> 1. **ANSWER SARA — banquet meal format. Oldest open item, still unanswered since 08-11.** Sara Lussier asked whether the banquet is **buffet or plated**, and whether the form should **collect meal choice**. The flyer advertises **"Steak, chicken or vegan dinner • $50 per person"**, so if that copy stands the form does need meal choice — confirm with catering before replying. Also: Sara framed BOTH registration links as questions to Steve ("Does this work?" / "Let me know if this works") and nobody has confirmed back to her; the links are live regardless, per Alex's go-ahead. Note the meal line is also the text you'd cut to fit `Registration required` on the flyer's banquet row, and 08-13's spacing fix left that row only **5px** of within-row clearance — **resolve Sara's question before touching that row.**
> 2. **Two small decisions Alex has been offered and hasn't made** (both one-liners, don't re-implement without a yes): (a) the **portrait poster's QR** still points at `/homecoming-2026` while the flyer + landscape poster now open `/#homecoming` — it's the lone inconsistency; (b) the **Events card ends with `Homecoming Poster` BELOW** the big `See the Whole Weekend Schedule` button — he may want the schedule button to be the literal last element.
> 3. **Above-the-fold is still unresolved.** Steve asked for "no scrolling"; `#events` is the first content section but the ~750px hero keeps it below the fold. Would need the hero shrunk — nobody has approved that.
> 4. **From the 08-12 call, NOT yet done (assigned to others, or out of scope for the site):** Steve to have Kate/Katha send the registration letter to faculty/staff with **embedded clickable links**; Meaghan to send Steve the **Sunday night event link**; Alex to **review Cloudflare Web Analytics** (never looked at). Alcohol at Oktoberfest was left OFF all collateral — it was only "probably a cash bar," too tentative to print. Name tags / staffing / dorm-lodging talk is internal logistics, deliberately not published.
> 5. **2026 HOF inductee photos + bios — PENDING (expected late Aug / after the Sept induction ceremony).** The Class of 2026 panel in the HOF box currently shows **names + grad years only** (Wildens Fils '15 · David Cady '94 · David Chase '85 · Anthony Moccia, honorary/in memoriam) with the note "Photos and full bios will be shared as they become available." When Steve / VTSU (Sara Lussier) send headshots + bios, swap the note for real content.
> 6. **Aldrich House section — LIVE with the redacted PDF linked + a sequential return-visit popup; still PENDING = the house photos.** Michael's letter references attached photos (house, front door, corner pilasters, newel post, radiator) but they are **NOT in the PDF** (the only image in it is the MVS logo) — need Michael/Alex to send the actual image files, then place them in the section (currently text-forward) and ideally swap the popup's placeholder image (currently the Manor Vail postcard).
>
> **⚠️ TWO LOCKED DECISIONS WERE REVERSED BY ALEX ON 08-13 — do not "restore" them:** (1) the Events card is **no longer limited to two actions** (it now carries 5 controls); (2) the landscape poster is **no longer Meaghan-only/undeployed** (it is tracked, public, and linked from the site's Poster button). The *print flyer* remains untracked and private.
>
> **Locked decisions (don't relitigate):** Spotlight is a **flat 3×3**, not themed triads (verified pool too meteorology-heavy — 7 mets, 1 business, 0 athletes). Mayo '90 & Priestley '09 badges are **Alex-approved ESTIMATES** (no public grad year). Benning shown as alumnus/judge only (he's LSC **Foundation** president — Foundation never tied to the donate flow). Mark Hilton excluded (2026 Distinguished Alumni role unverifiable publicly). Donate CTA copy/URL is sacrosanct — never "Give Now"/VSU-branded.
>
> **Env / gotchas:** Repo **syncs across machines** (Google Drive + Alex's **Command Center** dashboard, which now iframes the site via a `_headers` CSP `frame-ancestors` change) — history can interleave/rebase between sessions; consider `git pull` before editing. ⚠️ **The working tree can be STALE relative to HEAD at session start** — on 2026-08-12 the flyer/poster files on disk were a **Jul-28 snapshot** while HEAD was Aug-5, because Drive hadn't finished syncing; Drive caught up mid-session and silently replaced them, discarding edits made against the stale base. **Before editing, check `git status` AND file mtimes** — files showing as ` M` that you haven't touched are the tell, and they mean the disk is behind, not ahead. ✅ **PUSHING WORKS AGAIN — FIXED 2026-09-14. Plain `git push origin main` is the correct command now; ignore every older recipe.** **Root cause of every past failure:** `~/.gitconfig` routes GitHub auth through the **`gh` CLI** (`credential.https://github.com.helper = !’gh.exe’ auth git-credential`), which **shadows Git Credential Manager**. Whoever `gh` is logged in as *is* who `git push` is, regardless of what the browser is doing. It held only the **ICF** account `27698_icfcorp`, which is **read-only** here (`gh api repos/almaster1987/lyndon-alumni --jq .permissions` → `push:false`) — hence the 403s. Alex logged that out and signed in as **`almaster1987`**. **If a push 403s again, run `gh auth status` FIRST** — that is almost always the entire answer; `gh auth switch` silently changes who git is. ⚠️ **`gh auth login --hostname github.com --git-protocol https --web` is interactive — Alex must run it himself**: copy the one-time code, **press Enter**, authorize in a browser signed in as `almaster1987`, and wait for `✓ Logged in as almaster1987`. On 2026-09-14 it failed twice because the Enter step was skipped, leaving **no** valid account; git then fell through to a username/password prompt, and **GitHub removed password auth for git in 2021** — so `remote: Password authentication is not supported` means *the token is missing*, not that the password is wrong. Never tell him to type a password there. **Commit identity:** `user.email` is now set **repo-locally to `alexander.lataille@gmail.com`** (2026-09-14) so Council commits stop carrying the ICF address; his ICF email stays the global default. **Fallbacks if `gh` breaks again:** (a) a **fine-grained PAT** scoped to `lyndon-alumni` with Contents: Read+Write, entered via `gh auth login` → "Paste an authentication token"; (b) **GitHub web upload** at `https://github.com/almaster1987/lyndon-alumni/upload/main` — drag the changed files, commit directly to `main`. Web upload makes a **different commit** than any local one, so reconcile after: `git fetch origin`, confirm `git diff --stat <local-sha> origin/main` is empty, then `git reset --hard origin/main`. ⚠️ Claude’s own permission classifier may block the browser `file_upload` as a "Production Deploy" even after Alex says go — that gate is separate from credentials and only he can clear it in settings. Upload only what changed — same allowlist discipline as `git add`, since **`Homecoming-Flyer-2026-Print.*` and the two build scripts must stay untracked.** Git progress prints **red to stderr but isn't an error** — confirm via the `old..new main -> main` line, or just `git fetch && git log --oneline -1 origin/main`. If Git Bash throws a cygwin **"add_item ... fatal error"**, just retry. ⚠️ **Two build scripts now live in the repo root, UNTRACKED (added 2026-08-12 because this project has lost build scripts before — the old `_export-homecoming.ps1` vanished):** `_export-homecoming.py` renders all four artifacts (flyer, poster, print flyer, landscape poster) to PDF+PNG and asserts page count/size; `_build-print-flyer.py` regenerates `Homecoming-Flyer-2026-Print.html` **from** `Homecoming-Flyer-2026.html` (strips the 3 registration pills → plain `Registration required` / `RSVP requested` text, drops the store pill, repoints QR+footer to the homepage). **Re-run `_build-print-flyer.py` any time the main flyer changes, or the print copy silently goes stale.** Both need a local server on the port named at the top of `_export-homecoming.py`. Neither should be committed. ⚠️ Both generators do **string replacement against exact markup** — they `assert` on every substitution, so a reworded flyer makes them fail loudly rather than silently no-op; fix the pattern, don't delete the assert. ⚠️ **These HTML files are CRLF** — a Python script that splits on `"\n"` and compares whole lines will never match; normalize with `.replace("\r\n","\n")` first (this cost a rewrite on 08-12). ⚠️ **The flyer's Saturday column is a `flex` column with `flex:1` events** — if content overflows, trimming padding does NOT help (the events just redistribute the reclaimed space); you have to remove an actual line of wrapped text, **or raise one event's `flex` basis** (that's how 08-13 fixed the reception pill — but it steals height from the other rows, and the banquet row is now down to 5px). ⚠️ **`scrollWidth === clientWidth` does NOT detect text wrapping — only overflow.** On 08-13 this passed cleanly while "25-27" had silently wrapped to two lines on the landscape poster; the bad export was only caught by *looking at the rendered PNG*. **For any headline/lockup change: set `white-space:nowrap`, then compare each flex child's `right` edge against the container's `right` (the container is block-level, so its own width always equals the parent's and tells you nothing).** Always eyeball the exported PNG before committing type-size changes. ⚠️ **Re-running `_export-homecoming.py` rewrites all four PDFs even when only one HTML changed** — the untouched ones come back byte-different but identical in content (PDF metadata churn; the PNG stays identical, which is the tell). `git checkout --` those files rather than committing 1.8MB of noise. Repeated headless Chrome PDF/PNG exports can corrupt command/cmdlet lookup in the reused PowerShell terminal; use a **fresh terminal** or explicit executables (`C:\Progra~1\Git\cmd\git.exe`, `C:\Users\27698\AppData\Local\Programs\Python\Python311\python.exe`). For exports, use an isolated `--user-data-dir` and launch Chrome via `Start-Process -Wait`; invoking Chrome directly with `&` can return before files finish writing and leave stale artifacts in place. The in-app screenshot tool can crop/hang on letter canvases — trust DOM overflow checks plus the true 816×1056 exported PNG. Press-photo hotlinks (Valade @ Caledonian Record, Fitzgibbon @ Future plc CDN, Cantore/Gregory station photos) — confirm reuse rights before relying long-term; all keep `onerror` fallbacks. ⚠️ **The popup's mobile button stack is at its limit** — the 4-button stack once squeezed `.pres-scroll` to 0px and `.pres-card`'s `overflow:hidden` clipped the bottom two buttons off a 375×812 screen (fixed inside `@media(max-width:680px)`: `.pres-content{overflow-y:auto}` + `.pres-scroll{flex:0 0 auto;min-height:auto;overflow-y:visible}`). **If you add anything else to that stack, re-verify at 375px.** ⚠️ **The flyer's banquet row has only ~5px of within-row clearance** after the 08-13 spacing fix (`.day.saturday .event.breakfast-feature` flex 1.68; `.days` rows `1.28fr .72fr`) — fitting anything extra on that row now means cutting a line, and the only line available is the meal line still pending with Sara (open item 1). Sunday still has ~60px of slack.
>
> *(QC 2026-06-04 — `docs/QC-HANDOFF.md`. Analytics: Alex set up Cloudflare Web Analytics himself. Spotlight run notes: `fundraising/SPOTLIGHT-PLAN.md`.)*

## What this is

Static single-page site for the VTSU Lyndon Alumni Council (Lyndon Teachers College → Lyndon State College → NVU Lyndon → VTSU Lyndon, since 1911). Deployed via Cloudflare Pages (git-connected to the repo), **live at https://lyndon-alumni.pages.dev**. Source: `index.html` at root, no build step.

- **Upstream mirror:** https://github.com/almaster1987/lyndon-alumni
- **Owner / contact:** Alex Lataille (Alexander.Lataille@icf.com), member of the Council's Fundraising Committee.
- **Hosting accounts:** this project runs under Alex's *personal* Google identity — GitHub `almaster1987`, Cloudflare + Gmail `alexander.lataille@gmail.com` (NOT the ICF email). Council correspondence (e.g. Michael Thurston / Manor Vail Society, michaelvt2700@gmail.com) also goes via that Gmail.
- **Council president:** Steve Cormier (LSC '82).

## Donate flow — read carefully before touching the Give section

The donate CTA points to this Blackbaud Renxt form:

```
https://host.nxt.blackbaud.com/donor-form/?svcid=renxt&formId=fed909dd-635d-47ad-9868-24becf560417&envid=p-BzoGXjMynEG3LMXBAdBMug&zone=usa
```

Gifts go to the **Lyndon Alumni Association** directly — *not* to VSU/VTSU and *not* to the Lyndon State College Foundation.

### Rules (do not violate without asking)

1. **CTA copy:** "Donate to the Association" or "Donate to the Alumni Association." Never "Give Now," "Give to Lyndon," or anything VSU/VTSU-branded. Reviewer feedback in April 2026 was that "Give Now" looked like donating to the school.
2. **CTA styling:** prominent gold (not standard green). Pulsing in nav, gradient button on the donate card.
3. **No dollar-amount selector** on the donate card — the Blackbaud form collects amount on the next step. A selector here is redundant and was removed.
4. **The Lyndon State College Foundation is NOT the processor.** It's a separate 501(c)(3). Do not name it as the processor anywhere on the site. A plain "see also" link in the footer Resources block is fine — keep it disconnected from the donate flow.
5. **Foundation link** (https://www.lyndonstatecollegefoundation.com) lives in the footer Resources block only.

## Deploy

**Live host: Cloudflare Pages**, git-connected to the upstream repo. Every push to `main` auto-deploys — including via the one-click `push-to-github.bat`. No build step (build command: *none*, output dir: `/`). Security + cache headers come from the `_headers` file — **Cloudflare ignores `netlify.toml`**.

GitHub Pages was the prior host; it was **disabled on 2026-05-31** (Pages source set to *None*), so `lyndon-alumni.pages.dev` is the **sole** live URL — the old `almaster1987.github.io/lyndon-alumni` now 404s. (Source = None also stops pushes from silently re-publishing it.) `netlify.toml` is a dormant alternative config, not the live host.

**Pushing:** As of **2026-07-02**, `git push` **works from the sandbox** — GitHub credentials are cached (Git Credential Manager), so Claude can commit *and* push, and Cloudflare auto-deploys. Heads-up: git writes progress to **stderr**, so PowerShell renders it as red "RemoteException" text — that is **not an error**; confirm success by the `old..new  main -> main` line (and/or `git status -sb` showing in sync with `origin/main`). If a push ever hangs on a credential prompt, fall back to Alex running `git push` himself. (Prior note: pushing used to fail on a credential prompt — no longer the case.)

Local preview: open `index.html` in a browser, or `python -m http.server 8000`.

## Folder structure

```
Lyndon/
├── index.html            # the site
├── _headers              # Cloudflare Pages security + cache headers (LIVE config)
├── netlify.toml          # dormant Netlify config — NOT the live host
├── push-to-github.bat    # one-click commit + push (auto-deploys to Cloudflare)
├── README.md             # human-facing readme
├── CLAUDE.md             # this file — Claude Code auto-loads it
├── .gitignore
├── docs/
│   ├── CHANGELOG.md         # archived pre-Aug-2026 change log
│   ├── PROJECT_CONTEXT.md   # full project background
│   ├── DONATE_FLOW.md       # donate-specific rules + URL
│   └── SETUP.md             # onboarding on a new machine
├── fundraising/          # Council-internal (gitignored — never deployed)
└── lyndon-alumni-original/  # read-only reference clone (gitignored)
```

`lyndon-alumni-original/` is the cloned upstream repo for reference — **do not edit it**, and do not include it in deploys.

## When Alex asks for a change

- He's writing from inside the Council (Fundraising Committee), not as an outsider asking for background.
- Practical, action-oriented suggestions. He has authority on Council decisions but coordinates with John Kleinhans (Fundraising chair) and Steve Cormier (president).
- After making donate-related edits, double-check the URL hasn't drifted from the one above.

## Recent changes worth remembering

- **2026-09-14 (reception + banquet registration closed — `2f26d46`, `0938f01`, live and verified):** Per Steve’s "CLOSE REGISTRATION" email. **Site:** the 3 banquet controls and 2 reception RSVP controls became disabled `.btn-closed` buttons (new class — deliberately NOT `.btn-pending`, whose `:hover` repaints rust). Kept visible rather than deleted so the closure looks intentional instead of like a missing button. Events copy rewritten. **Flyer:** both `registration-pill` anchors → plain `.closed-note` text, footer updated, links 5→3 (store, golf, QR). **Landscape poster:** reception → "RSVP closed", banquet → "Registration closed" in a muted `.req-tag.closed` variant; golf keeps its gold marker; afternoon games stay unmarked; action band now names golf as the only open registration. **Untouched on purpose:** golf (3 links), golf sponsorship, donate (2) — all verified still present live. **Not updated:** the portrait poster (already printed). `_build-print-flyer.py` fixed — its two `kill_pill` calls had nothing left to strip and would have crashed the next build. ⚠️ **The Blackbaud forms themselves remain OPEN** — removing links is not closing registration; Sara Lussier/VTSU has to close them, and it is unconfirmed whether that happened. **Auth fixed the same day:** `gh` logged out of ICF and into `almaster1987`, so plain `git push` works again; repo-local `user.email` set to the Gmail address. Alex declined adding the ICF account as a repo collaborator (enterprise-managed, may not even be permitted as an outside collaborator, and it ties an employer identity to a volunteer project).

- **2026-09-08 (Steve's Sunday-soccer edit — shipped `8e3d187`, live and verified):** Steve emailed "Can you add to the 3:30 Sunday Men's and Women's coed alumni game. Add Soccer" — that row now reads **"Men's & women's co-ed alumni soccer game"** on `Homecoming-Flyer-2026.html` and, via `_build-print-flyer.py`, on the private print copy. It was the only place the phrase existed (neither poster carries it). Verified in the exported PNG (wraps to two lines, Sunday still has room) and in the live PDF's extracted text — ⚠️ note a naive `count("co-ed alumni soccer game")` returns **0** because the wrap splits the phrase across text spans; grep the lines instead. All four PDFs still one page at the right sizes, flyer keeps exactly 5 links, live PDF SHA-256 byte-identical on both domains. **⚠️ Deployment story changed:** the GCM push recipe added on 08-13 **stopped working** (GCM now opens an interactive dialog; no TTY), and Alex has **signed out of `almaster1987` in `gh` and wants to stay signed out**. The ICF account is **verified read-only** on the repo (`push:false`), so neither the CLI nor a browser signed in as ICF can deploy. It shipped instead by **GitHub web upload driven through Claude-in-Chrome** once Alex switched Chrome to `almaster1987` — no credentials, ICF account untouched. That path is now the documented fallback; it makes a separate commit, so the local branch was reconciled by confirming identical trees then `git reset --hard origin/main`.

- **2026-08-13 (Alex's revisions to the 08-12 edits — three commits, all pushed live and verified on both domains):** (1) **`3381376` — Homecoming actions split per event.** Popup and Events card both now carry `Register for Golf` / `Register for the HOF Banquet` (rust) / `RSVP for the Welcome Reception` (new green `.btn-rsvp`) / larger full-width `See the Whole Weekend Schedule` (new `.btn-wide`); site Poster button repointed to the landscape poster. **Reversed two locked decisions at Alex's explicit request:** the Events card is no longer two-actions-only, and the landscape poster is no longer undeployed. Also **fixed a genuine mobile defect this introduced** — the taller stack collapsed the popup's nested `.pres-scroll` to 0px and the `overflow:hidden` card clipped the bottom two buttons off a 375×812 screen; short viewports now scroll `.pres-content` instead. **Flyer spacing:** the Welcome Reception RSVP pill was overflowing its box padding by 9px (2.1px to the border) → now 18.2px via `breakfast-feature` flex 1.42→1.68; the Oktoberfest→check-in gap was literally 0px → now 28.4px via `.days` rows `1.1fr .9fr`→`1.28fr .72fr`, taken from Sunday's 64px of slack. (2) **`a5e9b56` — QRs now open the site popup.** Flyer, landscape poster and print flyer all decode to `https://lyndonalumni.com/#homecoming`; `index.html` opens the popup instantly on that hash rather than after the 900ms delay. `/homecoming-2026` still resolves, so printed collateral keeps working. Landscape poster rail collapsed (`.highlights` `flex:1`→content-sized; 5.71in→3.85in) so the cards stopped carrying ~1in of dead space each. Hardened `_build-print-flyer.py`: its QR/footer substitutions were plain `str.replace` with no assert and would have silently no-opped, staling the print copy. (3) **`613e0fa` — per-event markers + bigger 2026 on the landscape poster.** Golf and awards banquet got `Registration required`, welcome reception `RSVP encouraged`, afternoon games left unmarked; each tag sits under its own time line so golf's marker can't be misread as covering the free Oktoberfest below it. `2026` now matches `25-27` — at the literal 98px the pair **silently wrapped "25-27" onto two lines** (caught only by viewing the exported PNG, since `scrollWidth===clientWidth` passes on wrapped text), so both sit at 88px with `nowrap`; place line 10→12.5px, `.left-col` 5.35→5.55in. **Validation each round:** all four PDFs one page at 612×792 / 792×612, PNGs 816×1056 / 1056×816, live flyer exactly 5 links and the posters/print flyer zero, all QRs decoded at 144/192/300 DPI, donate `formId` untouched, deploys allowlisted (never `git add -A`) and live PDFs SHA-256 byte-identical to local. **`Homecoming-Flyer-2026-Print.*` remains untracked and verified unpublished.** Also corrected the handoff's wrong claim that Claude can't push — see the push recipe in the banner.

- **2026-08-12 (Council call implemented: Homecoming to the top, Register/RSVP split, store link, two Meaghan-only artifacts — pushed live, commit `452e23a`):** Worked from the 08-12 call transcript (Alex + Steve Cormier + Meaghan Meachem). Alex approved a written change-table before any edits. (1) **`#events` moved above `#about`** so Homecoming/Golf lead the page — it carries the HOF box and `#awards` with it; `#aldrich` gained `sec-alt` because the move left it and `#spotlight` both plain white. (2) **Terminology split per Steve:** paid = **Register** (golf, HOF banquet), free = **RSVP** (morning reception). (3) **`Registration required`** added to the poster's scan band and throughout the print flyer, but **deliberately not** on the live flyer's golf/banquet rows — it overflowed the Saturday flex column, and the only text available to cut is the meal line that's still under discussion with Sara. Rule adopted: *the marker appears wherever there is no clickable Register control.* (4) **Lyndon Store pill went live in gold** (Alex: deep green too dark) — **the URL Meaghan supplied carried a Google Forms edit-your-response token that would have let any scanner edit a submitted order; stripped to the canonical `/d/e/…/viewform`.** (5) **MEISA expanded** to Music Entertainment Industry Student Association (Meaghan confirmed on the call — the old "don't guess" caveat is retired). (6) **Check-in block** added to the flyer: Theater Lobby, Fri 6–7:30 PM, Sat from 8:30 AM. (7) **Two new UNTRACKED artifacts for Meaghan only** — `Homecoming-Poster-2026-Landscape.*` (11×8.5 for campus TVs; purpose-built re-layout, not a reflow of the portrait poster — two columns, president card + 2×2 highlight grid) and `Homecoming-Flyer-2026-Print.*` (zero links, QR→homepage, for local businesses). **Validation:** all four PDFs one page at the right size (612×792 / 792×612), PNGs 816×1056 / 1056×816, live flyer carries exactly 5 clickable links and the print flyer/posters carry **zero**, QRs decode at 144/192/300 DPI. **Live-verified on both domains:** events precedes about in the served HTML, banquet button + formId present, MEISA spelled out, no "Octoberfest" typo, both PDFs byte-identical to local, `/CLAUDE.md` + `/docs/*` still 301, and **both Meaghan-only files plus `fundraising/`, `Meeting Transcripts/`, `Archive/` and the meeting `.docx` all return the 97,567-byte homepage fallback rather than the file.** Deploy allowlisted to 7 files. **Not done (see open items):** Homecoming is the first content section but still below a ~750px hero, so not literally above the fold.

- **2026-08-12 (Friday Oktoberfest + all three RSVPs live — pushed live, commit `69bdd26`):** Two rounds of Council input landed. (1) **Meaghan's reframe of the Saturday 9:30–11:30 block** — from a class-reunion breakfast to a **Welcome Reception for President Kollmann** "for Alumni, Families, Faculty, staff and Administration both current and former," with classes (including the Class of 1976 50th) celebrated as the secondary note. Flyer carries the full invite list; the poster's four-column highlight band can't fit it, so the poster reads **Welcome Reception / For President Kollmann • 9:30-11:30 AM / Alumni, families, faculty & staff / Class of 1976 • 50th Reunion** ("administration" and "current and former" deliberately dropped there for space). (2) **Friday's "Evening social / Details to be announced" became Oktoberfest** — 7 PM, Veterans Park, hosted by MEISA, live music + beer garden + bratwurst. Full detail on the flyer; poster shows it as the Golf column's secondary line; site gets a new act line plus a popup clause. Fixed the pre-existing **"Octoberfest" → "Oktoberfest"** misspelling. (3) **Sara Lussier sent both remaining Blackbaud links** (08-11), so the reception pill on the flyer, the banquet pill on the flyer, and the site's disabled `btn-pending` **Banquet RSVP** button all became real links; the site button is now an `<a target="_blank" rel="noopener">`. **`.btn-pending` CSS (index.html ~line 600) is now unused but deliberately kept** — this project cycles controls in and out of pending states. **Validation:** no overflow/clipping on either sheet at any measured point; adding the → arrow did **not** wrap the banquet pill (height still 18px, bottom clearance 18.8px); site clean at desktop and 375px (banquet button wraps to 245×69 inside the HOF box, zero horizontal overflow); donate `formId` still `fed909dd…`. **Live-verified on BOTH domains:** Oktoberfest/Veterans Park/MEISA and the banquet formId all present, "Octoberfest" gone, flyer PDF at `/homecoming-2026` and the poster PDF both **byte-identical to local** (flyer sha256 `0517c92c4746dd2c15daa11da8a6f7982f06233a40c8ef91ad1996727ddc13a3`, poster `7ad36097bde9e60cd6e206875a37e20f4c9d4d9d4bfcc771565e4804af9f5578`), `/CLAUDE.md` + `/docs/*` still **301**, and `fundraising/`, the unredacted Aldrich letter, `Meeting Transcripts/`, `Archive/` all return the **97,519-byte homepage fallback** (not the file). Deploy was **allowlisted to the 7 intended files** — never `git add -A`. **Export recipe (no script is tracked):** serve the repo with `python -m http.server`, then headless Chrome `--print-to-pdf` with an isolated `--user-data-dir` + `--virtual-time-budget=20000` + `--no-pdf-header-footer`, then rasterize page 1 with **PyMuPDF at `dpi=96`** — US Letter at 96 DPI is exactly the 816×1056 PNG the project expects. Verify QR with `cv2.QRCodeDetector` at 144/192/300 DPI and check clickable links via `page.get_links()`.

- **2026-08-05 (red registration actions + flyer store placeholder — pushed live):** Changed Homecoming registration actions on the popup, Events section, golf card, Hall of Fame box, and detailed flyer from green/gold to the existing rust-red brand treatment so they stand out from general navigation. Replaced the flyer heading's “All events are in Lyndonville unless noted” line with the compact **Lyndon gear: Available Saturday during the games** callout and an intentionally inactive green **Lyndon Store** pill. The merchandise promotion remains absent from the popup and Events card. Regenerated only the flyer PDF/PNG. **Validation:** no source diagnostics; phone-width web buttons have white text and no local overflow; flyer is a one-page 8.5×11 PDF with an 816×1056 PNG; gear/store controls are visibly rendered; QR decodes to the hosted flyer route at 144/192/300 DPI.

- **2026-08-05 (Homecoming registration hierarchy — pushed live):** Removed the Saturday Lyndon-gear promotion from the first-visit popup, Events card, and detailed flyer. Reworked the Homecoming Events actions into a clear two-step stack: **Register for the Full Weekend** (hosted flyer) with **Homecoming Poster** beneath it, plus copy naming golf, welcome-reception, and banquet registration. Replaced the site's gray banquet placeholder and the flyer's “coming soon” lines with deep-green **“RSVP: Let us know you're coming”** controls; they remain intentionally unlinked pending Sara's URLs. The flyer golf pill links to the live Blackbaud registration form. Removed the Sunday “Make a weekend of it” box; changed the flyer footer to list all three registration paths; and pointed both flyer/poster QRs to `https://lyndonalumni.com/homecoming-2026`. Regenerated both PDF/PNG pairs using isolated, explicitly waited Chrome processes. **Validation:** source diagnostics and diff checks clear; web controls have no local overflow at desktop/phone widths; PDFs are one-page US Letter; PNGs are 816×1056; both PDF QRs decode to the exact flyer route at 144/192/300 DPI.

- **2026-08-05 (final detailed-flyer hierarchy + custom-domain QR — pushed live):** Consolidated the merchandise message into a compact **Lyndon gear** callout beside the Weekend Lineup heading (available Saturday during the games; online shop link still coming soon), folded the free-breakfast/RSVP status into the reception details, removed the redundant Friday golf-registration note, and gave Sunday's return-home note a restrained rust accent. The footer's visible URL and framed QR now both target **`https://lyndonalumni.com/#events`**. Regenerated the flyer PDF/PNG; validation passed with no editor or diff diagnostics, an 816×1056 PNG, a one-page 8.5×11 PDF, and an exact QR decode to the custom-domain Events anchor. The main site and hallway poster were unchanged in this deployment.

- **2026-08-04 (detailed-flyer QR correction — validated, ready to push):** Changed the bottom-right flyer QR and visible URL from the self-referential `/homecoming-2026` PDF route to **`https://lyndon-alumni.pages.dev/#events`**. Fixed the clipped gold frame by reducing the QR/grid column from 1in to **.88in** and explicitly centering it; rendered geometry = 84×84px code box inside a ~103px footer, with **6.11px top / 6.13px bottom frame clearance** and a 0.01px vertical-center delta. Regenerated the flyer PDF/PNG via local Chrome. **Validation:** HTML diagnostics clear; no Letter-sheet overflow; visual export shows the complete centered frame; PNG = 816×1056 and decodes to the exact Events URL; PDF = one 8.5×11 page and decodes at 144/192/300 DPI from multiple footer crops. The legacy VS Code validator's hard-coded narrow flyer crop is still not authoritative for the moved/framed code.

- **2026-08-04 (Homecoming call staging — validated, ready to push):** Based on Alex + Steve's catch-up, synchronized the web and print surfaces around the **Welcome Reception for President Sherry Kollmann** (Saturday, 9:30–11:30 AM): corrected the site spelling, replaced the old summary, and retained the Class of 1976 / Q&A context. Added a compact **Shop Lyndon gear** callout to the Homecoming popup/card and flyer: **Mark Parkinson '98** will sell Lyndon State College and NVU Lyndon merchandise at the soccer field, **Saturday noon–5 PM**. Added native disabled site controls for the pending **Breakfast RSVP** and **Banquet Registration** URLs; the flyer plainly says "RSVP link coming soon" and "Registration link coming soon" rather than inventing destinations. The existing local poster revision uses the matching Welcome Reception language. **Validation:** editor diagnostics clear; the changed desktop/mobile card surfaces have no local overflow; flyer and poster PDFs are one-page US Letter, PNGs are 816×1056, and both QR destinations decode to `https://lyndonalumni.com/homecoming-2026` from their complete footer regions. **Export gotcha:** the VS Code task `Final Homecoming artifact validation` uses an overly narrow hard-coded flyer crop which cuts into the now-framed code and reports a false negative; validate the complete footer QR crop instead. The old `_export-homecoming.ps1` is absent; local Chrome headless export plus an 864×1104 full-canvas screenshot cropped 24px on each edge produced the final flyer assets.

Older entries (2026-04-27 through 2026-07-27) live in [`docs/CHANGELOG.md`](docs/CHANGELOG.md).
